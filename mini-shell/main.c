#include <stdio.h>
#include <string.h>

#include "functions.h"

int main()
{
  char *buffer;
  while (1)
  {
  }
  return 0;
}

int getcommand(const char *buffer)
{
  int c;
  while((c = getchar()) != '\n' && c != EOF)
  {

  }
  return 0;
}

